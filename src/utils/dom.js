/**
 * @module JSUTILS
 */

var JSUTILS;
if (!JSUTILS) JSUTILS = {};

/**
 * Javascript utilities for working with DOM
 * @class Dom
 */
JSUTILS.Dom = (function (doc) {
    
    "use strict";

    // Public instance of the class
    var U = {};

    return U;
}(document || this))