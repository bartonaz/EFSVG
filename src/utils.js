/**
 * Module with the general purpose Javascript utilities
 * @module JSUTILS
 * @main JSUTILS
 */

var JSUTILS;
if (!JSUTILS) JSUTILS = {};


// // List of properties from the Gen class to be set directly to the Module
// var childList = [ "typeOf", "isArray", "forEachIn", "resForEachIn" ];
// // Setting Main class properties as public module properties
// JSUTILS.Gen.forEachIn(childList, function (prop) {
//     JSUTILS[prop] = JSUTILS.Gen[prop];
// });
